### 🚀 Quick Start

# 1. Клонировать репозиторий

```
git clone https://github.com/RicoVKosmose/3Dbuilding.git
cd 3Dbuilding
```

# 2. Создать виртуальное окружение (рекомендуется) Windows

```

python -m venv venv
venv\Scripts\activate

```

# 3. Установить зависимости

```

pip install -r requirements.txt`

```

# 4. В переменных средах добавиить путь в `PATH` для `colmap/bin`

<img width="1851" height="582" alt="image" src="https://github.com/user-attachments/assets/f4fd027c-1802-4195-ba94-0328731254f1" />

# 5. Добавить изображения

Поместите фотографии в папку:

`data/images/`

Рекомендуется загружать фотографии в папку data/images.
Это нужно для того, чтобы итоговые файлы реконструкции автоматически сохранялись в правильные папки (sparse, dense) и структура проекта оставалась аккуратной.
Если выбрать другую папку, выходные файлы будут создаваться рядом с выбранными изображениями, что нарушит порядок проекта.


# 6. Запустите приложение

```
python main_gui.py
```


# Требования:

- `Python 3.9 – 3.12`
- ОС: `Windows `
- Наличие видеокарты (рекомендуется `NVIDIA`) и соотвествующие драйвера.
